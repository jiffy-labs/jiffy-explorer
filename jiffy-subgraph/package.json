{
  "name": "aa-subgraphs-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ lazycoder1/aa-subgraphs-test",
    "create-local": "graph create --node http://localhost:8020/ lazycoder1/aa-subgraphs-test",
    "remove-local": "graph remove --node http://localhost:8020/ lazycoder1/aa-subgraphs-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lazycoder1/aa-subgraphs-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.0",
    "@graphprotocol/graph-ts": "0.29.0"
  }
}
